// One Keep Metrics deployment = one client engagement (that's why the gate
// page never names the client - the URL itself is the only thing that says
// "this is for you"). Swap CLIENT when standing this up for a different client.
const CLIENT = "afd";
